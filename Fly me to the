#include <iostream>

using namespace std;



int main()
{
	int t, x, y, j, sum;

	cin >> t;

	for (; t > 0; t--) {
		cin >> x >> y;

		for (sum = 0, j = 0; ; j++)
			sum += 2 * j;

		if (sum == y - x)
			cout << 2 * (j - 1);
		else
			cout << 2 * (j - 1) + 1 << endl;
	}

	return 0;
}
